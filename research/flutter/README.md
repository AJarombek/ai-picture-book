### Commands

**MacOS Setup**

```bash
brew install --cask flutter

# Verify that flutter & dart are installed
which flutter dart

# Check to see if there are any missing flutter dependencies
flutter doctor

# Create the sample flutter app
flutter create sample_app

# Run the sample app
cd sample_app
flutter run
```